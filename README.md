# NetPulse WPF + FastAPI

MVP repository layout:

- `client/NetPulse.Client`: Windows WPF MVVM client
- `backend`: FastAPI discovery backend + SQLite storage
- `docs`: API contract, architecture, and setup

See `docs/setup.md` to run locally.
